# === Google Cloud Project Settings ===
# Your GCP project ID (auto-detected from gcloud config if not set)
# PROJECT_ID=your-project-id

# GCP region for all resources
REGION=us-central1

# === Resource Names ===
GATEWAY_NAME=demo-agent-gateway
MCP_SERVICE_NAME=finance-mcp-server
AGENT_REGISTRY_SERVICE_NAME=finance-mcp-service
AGENT_DISPLAY_NAME=demo-agent-governed
DENY_POLICY_NAME=mcp-read-only-policy

# === Gemini Model ===
GEMINI_MODEL=gemini-3-flash-preview
